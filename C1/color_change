--write these into the file named "ColourEdit" in a part called "ColourChangeBlock"
  
--add this is the first line
local part = script.Parent

--add this line after the "while" line
	part.Color = Color3.new(255,255,255)
	--waits can be added in this manner
	wait(2)
	part.Color = Color3.new(255,255,255) 
	--and so on matching number of waits the kid wants
